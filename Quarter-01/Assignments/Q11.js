var names = ["Ayaan", "Sabah", "Ali", "Areesh", "Fatimah"]; // Declare an array named 'names' of type string and assign it the values ["Ayaan", "Sabah", "Ali", "Areesh", "Fatimah"]
for (var i = 0; i < names.length; i++) { // Start a for loop with a loop counter 'i' initialized to 0, looping as long as 'i' is less than the length of the 'names' array, incrementing 'i' by 1 after each iteration
    console.log(names[i]); // Log the element at index 'i' of the 'names' array to the console
}
