var car = "Mehran";
// Declare and initialize a variable called 'car' with the string value "Mehran".
var age = 23;
// Declare and initialize a variable called 'age' with the numeric value 23.
var city = "karachi";
// Declare and initialize a variable called 'city' with the string value "karachi".
var score = 85;
// Declare and initialize a variable called 'score' with the numeric value 85.
var fruit = ['Apple', 'Banana', 'Mango'];
// Declare and initialize an array called 'fruit' with the string values "Apple", "Banana", and "Mango".
// True
console.log(car == "Mehran");
// Log the result of the comparison 'car == "Mehran"' to the console. This will log 'true' because 'car' is equal to "Mehran".
console.log(age > 20);
// Log the result of the comparison 'age > 20' to the console. This will log 'true' because 'age' (23) is greater than 20.
console.log(city == "karachi");
// Log the result of the comparison 'city == "karachi"' to the console. This will log 'true' because 'city' is equal to "karachi".
console.log(score <= 85);
// Log the result of the comparison 'score <= 85' to the console. This will log 'true' because 'score' (85) is less than or equal to 85.
console.log(fruit.indexOf("Banana") == 1);
// Log the result of the comparison 'fruit.indexOf("Banana") == 1' to the console. This will log 'true' because the index of "Banana" in the 'fruit' array is 1.
// False
console.log(car == "Honda");
// Log the result of the comparison 'car == "Honda"' to the console. This will log 'false' because 'car' is "Mehran" and not "Honda".
console.log(age < 10);
// Log the result of the comparison 'age < 10' to the console. This will log 'false' because 'age' (23) is not less than 10.
console.log(city == "Lahore");
// Log the result of the comparison 'city == "Lahore"' to the console. This will log 'false' because 'city' is "karachi" and not "Lahore".
console.log(score >= 15);
// Log the result of the comparison 'score >= 15' to the console. This will log 'true' because 'score' (85) is greater than or equal to 15.
console.log(fruit.indexOf("Orange") === -1);
// Log the result of the comparison 'fruit.indexOf("Orange") === -1' to the console. This will log 'true' because "Orange" is not in the 'fruit' array, so indexOf returns -1.
